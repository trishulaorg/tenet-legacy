type ImageMimeType = 'image/jpeg' | 'image/png' | 'image/svg+xml'

const IMAGE_MIME_TYPE: ImageMimeType[] = ['image/jpeg', 'image/png', 'image/svg+xml']

export { IMAGE_MIME_TYPE }
export type { ImageMimeType }

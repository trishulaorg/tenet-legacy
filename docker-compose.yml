version: '3'
services:
  web:
    build .
    ports:
      - "8080:8080"
  db:
    image: postgres:13-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin

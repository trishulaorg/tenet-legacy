/* eslint-disable */
import React from 'react'

type Props = {
  createdAt: Date
}

export default function Timestamp(props: Props) {
  const { createdAt } = props

  return <div>{/* TODO */}</div>
}

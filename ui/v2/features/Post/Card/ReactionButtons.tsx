/* eslint-disable */
import React from 'react'

type Props = {
  likesCount: number
  repliesCount: number
}

export default function ReactionButtons(props: Props) {
  eslint - disable - next - line
  const {} = props

  return <div>{/* TODO */}</div>
}

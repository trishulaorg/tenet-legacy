/* eslint-disable */
import React from 'react'

type Props = {
  id: string
  name: string
}

export default function UserName(props: Props) {
  const { id, name } = props

  return <div>{/* TODO */}</div>
}
